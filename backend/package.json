{
  "name": "autosoil-logger-backend",
  "version": "2.0.0",
  "description": "AutoSoil Logger Backend - LangGraph Orchestrator",
  "main": "app/main.py",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
